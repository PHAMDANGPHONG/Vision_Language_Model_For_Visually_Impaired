"""Evaluation utilities.

Subdirs:
    run_benchmark.py   — latency / RAM measurement on N samples
    eval_vqa.py        — VQAv2 / VizWiz accuracy
    eval_captioning.py — BLEU / METEOR / CIDEr / BERTScore
    eval_pope.py       — Object hallucination POPE benchmark
    eval_ablation.py   — Compare 4 configs (baseline, +L1, +L1+L2, full)
    demo_analysis.py   — (Week 15) Qualitative case-study on 30-50 self-shot
                         room images: hit / near-miss / critical classification.
"""
